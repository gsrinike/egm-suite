# gui.rcc.manager

Vue-based RCC manager. The first increment implements CSA case setup, CSA result
inspection, and workflow monitoring. CC and OPC are visible as disabled
capability placeholders for later increments.

Run locally:

```bash
npm install
npm run dev
```
