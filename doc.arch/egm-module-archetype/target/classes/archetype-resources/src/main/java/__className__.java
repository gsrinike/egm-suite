package ${package};

/**
 * Initial generated module class.
 *
 * Replace this class with capability-specific code after adding the module to
 * the root Maven reactor.
 */
public class ${className} {
    public String moduleName() {
        return "${artifactId}";
    }
}
