/**
 * Vault-backed secret resolution for application configuration.
 *
 * <p>The module depends on {@code com.utils} authorization utilities so secret
 * access can be validated before a value is returned.</p>
 */
package com.vault;
