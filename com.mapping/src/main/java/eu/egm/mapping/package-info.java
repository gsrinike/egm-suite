/**
 * Generic mapping primitives shared by domain mapping modules.
 *
 * <p>This module contains reusable mapping metadata and services only; domain
 * rules belong in the consuming map/data modules.</p>
 */
package eu.egm.mapping;
