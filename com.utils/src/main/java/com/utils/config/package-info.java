/**
 * YAML configuration loading and Spring environment post-processing.
 *
 * <p>The loader resolves module-specific base and environment configuration
 * before application beans are created.</p>
 */
package com.utils.config;
